# Shard Live Learning by single-owner Class Session

Exactly one epoch-bound Session Owner will order each active Class Session, with a Presenter Lease preventing dual control. Zero-Cash Production runs one owner process, while funded deployments scale by assigning different sessions to different replicas; durable educational interactions commit through PostgreSQL/outbox, and high-rate presence, pointer, and viewport state remains ephemeral.
