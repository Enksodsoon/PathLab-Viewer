This directory is intentionally empty of Caddy fragments. Local Compose starts
without qualification routes. Production uses a separate root-owned directory
under `/etc/pathlab-viewer`; never commit credentials or live infrastructure
configuration here.
