# Production Qualification

Capacity authority: [ADR 0132](../adr/0132-qualify-3000-learner-combined-broadcast-with-zero-cash-admission.md) controls the 3,000-learner combined target, media quality, zero-cash admission and safe partial-delivery rules. Existing operating limits remain unchanged until implementation and fresh qualification.

Production qualification binds evidence to one complete immutable candidate fingerprint, one deployment profile, one host/resource declaration, one independent backup target, one client matrix, one standards corpus, one campaign manifest, and the current `DeploymentSelectionReceipt(SELECTED)` head that actually routes that release. Reusing a historical result, changing a pin or host silently, combining successes from different candidates, or retaining admission after the deployment-selection head changes is prohibited. The [Delivery State Ledger](./DELIVERY_STATE_LEDGER.md) defines lifecycle states and receipts; this document defines the gate register whose current results may be aggregated into a `PRODUCTION_QUALIFIED` receipt.

## Evidence and result model

Each gate emits exactly one `SUCCESS`, `PARTIAL`, `NEGATIVE`, or `NOT_EVALUABLE` result. `SUCCESS` means every frozen threshold, behavioral invariant, cleanup step, and recovery check passed on the exact subject. A missing artifact is `NOT_EVALUABLE`; an incomplete workload is `PARTIAL`; and an invariant or threshold breach is `NEGATIVE`. No result from another commit, host, profile, workload, or evidence window can fill a gap except unaffected historical long-duration durability evidence carried by a signed unchanged-input equivalence declaration. That declaration must prove identical relevant artifacts, configuration, host/storage/backup-target inputs, workload, operators, controls, and evidence schema; it cannot retain candidate admission. Any `DeploymentSelectionReceipt` head change requires a new P6-T30 admission, and the exact final candidate still completes trusted `soak_end - soak_start >= 14 * 24h` with no uncovered instant plus every affected or targeted rerun.

`CLINICALLY_QUALIFIED` remains a separate purpose-specific status. Base Clinical Shadow interoperability, self-conformance evidence, `PRODUCTION_QUALIFIED`, or `ACTIVATED` does not authorize diagnostic, patient-care, or clinical use.

## Mandatory gate register

| Gate | Frozen workload and success boundary |
| --- | --- |
| License, provenance, security and offline build | Every mandatory source, binary, model, standard artifact, font, icon, and data/imagery asset has an admitted license or rights record, exact source/revision, checksum, notices, SPDX and CycloneDX inventories, and reproducible provenance. An Institution-owned clean ARM64 runner rebuilds or assembles and verifies the signed offline release kit without a hosted dependency. OWASP ASVS 5.0.0 Level 2 controls are mapped to PathLab and the rest-server web surface with explicit N/A evidence; a reachable Critical blocks immediately and a High requires a verified mitigation expiring within 30 days. Production, backup, build, and restore paths have default-deny system egress. |
| Runtime, modes, keys and lightweight scale control | On the declared 2-OCPU/12-GB host: clean install, upgrade, rollback, offline reinstall, immutable release switch, PostgreSQL migration, SELinux policy, systemd credential loading, cgroup caps, 80-percent throttle, 90-percent shed, Safety Shutdown, and restart throttling all succeed. AB, AC, and BC two-of-three root recovery, operator replacement, host-loss rewrap, service/purpose/signing-key rotation across retained data, and loss of one share all succeed without exposing root material. Exactly one heavy Mode Reservation runs; protected resident work and emergency headroom remain available. The lightweight cell also proves transient-versus-sustained 69.9/70/90-percent scale signals and hysteresis, exactly one current Institution home-cell route with absent/ambiguous/stale mappings failing closed, per-context extraction without cross-context SQL, and quiesced relocation with pre-switch reversal, one routing-epoch switch, post-switch reconciliation and no dual write or lost authority. A funded cell, HA/DR, automatic failover, larger capacity or provider spend requires its own authorized implementation, deployment and qualification and cannot inherit this profile's Zero-Cash result. |
| Trust, roles and Learning Catalog | Exercise the exact [Role and Approval Matrix](./ROLE_APPROVAL_MATRIX.md) with at least 10,000 Principals, all role combinations and incompatibilities, self-approval attacks, step-up expiry, two-person actions, last-owner protection, learners without email, guardian/minor rules, withdrawal, recovery, and cross-Institution attacks. The same candidate handles 100 Courses and at least 100,000 Course Version, Enrollment, roster/import, Learner Progress Evidence, Completion Evidence, Achievement Definition Version, and snapshot rows without transferring Catalog authority. |
| Governance, retention and deletion | Exercise every retention trigger frozen in [Governed Product Workflows](./GOVERNED_PRODUCT_WORKFLOWS.md), Institution schedules below product ceilings, Legal Hold creation/expiry, Processing and Transfer Grant expiry/withdrawal, subject export, and a fail-closed Deletion Saga. Every owner returns a Deletion Receipt covering authoritative records, projections, indexes, exports, derivatives, credential revocation, public-copy warnings, backup obligations, and per-purpose/object key crypto-erasure. Deduplicated physical backup bytes are never falsely described as immediately erased. |
| Viewer, Library and Imaging | Use the actual maximum-admitted live corpus under the dynamic Storage Admission Ledger, including a maximum admitted source, resumable upload, corruption/decompression-bomb cases, static DZI publication, search/folders/collections/saved views/Trash/thumbnails, Restricted Share revocation, reviewed Public Release, 25,000-object/50-MB Annotation Layer Versions, calibration, restore, and the supported physical client/accessibility matrix. Hashes, privacy state, authority, and route behavior survive restart and migration. |
| Teacher AI | Each admitted device tier runs one frozen corpus of at least 300 representative authoring tasks reviewed independently by two qualified reviewers. Meet the frozen quality, approved-source, refusal, no-publication/no-grading, download, memory, latency, soak, airplane-reload, zero-egress, corruption, atomic rollback, cache-eviction, and prompt-injection thresholds. Deterministic templates remain usable when the local bundle is unavailable; TRACE-SIM remains outside production qualification and activation. |
| Live Learning | One Instructor plus 3,000 learners completes a 60-minute campaign over six DZI slides at the qualified ephemeral pointer/viewport rate, six all-response prompts, 20-percent submitted-question traffic, 10-percent reconnect, a `pathlab-live` restart, and final convergence. Guests create no durable learner evidence; notebooks remain device-local unless explicitly submitted; prompts, polls, submitted questions/responses, selected submitted workspace actions, and Attendance Intervals persist with exact roster/content snapshots. Repeat the product campaign with every learner receiving teacher audio/video under ADR 0132. |
| Teacher Broadcast | One Instructor broadcasts client-encoded VP8/Opus at the frozen 540p envelope to 3,000 receive-only viewers for 60 minutes. Prove token and Institution isolation, direct and TURN behavior, no recording or server transcoding, bounded bandwidth/CPU/memory, reconnect, Galene failure, and automatic synchronized slides/text Media Fallback without loss of durable Live Learning state. |
| Assessment | 300 learners complete 100 items over 120 minutes, revising at least every 30 seconds with 10-percent disconnect, process restart, one-device lease and audited staff-authorized transfer, deadline sealing, and every learner submitting in the final 60 seconds with zero accepted-data loss. The corpus covers single choice, multiple choice, true/false, numeric, short text, essay, hotspot, shared stimulus, and native WSI point/region items; accommodations, deterministic randomization, deterministic/manual scoring, moderation, two-person high-stakes grade change, and 30-day appeal pass. AI scoring, adaptive testing, default negative marking, webcam capture, and screen-surveillance proctoring remain absent. |
| Learning and credential interoperability | Official/reference, adversarial, and at least two independent implementation fixtures exercise every frozen LTI 1.3/Advantage, OneRoster, QTI, CASE, Open Badges 3.0, CLR 2.0, and optional Caliper profile. Process at least 100,000 roster records, 10,000 QTI Items, and 10,000 Achievement Credentials—each threshold is independent. Test outages, retries, replay, key rotation, subject binding, mapping ambiguity, disabled imports, quarantine, grade return, credential status, and optional-delivery failure without changing Catalog, Assessment, Trust, or Credential Ledger authority. The claim is document/profile self-conformance only; it does not claim a public Host/API, paid certification, or universal interoperability. |
| Credential Ledger | At least 10,000 issuance, authenticated and offline verification, supersession, expiry, and revocation operations use immutable Achievement Definition Versions and accepted eligibility evidence. Prove step-up, two-person credential-bearing authorization, opaque subjects, private bounded OB3/CLR2 exchange, exact signature/canonicalization processing, Bitstring Status List `statusSize=1`, at least 131,072 expanded entries, canonical in-bound indices and undersized/out-of-range/bomb rejection, signing-key rotation, tamper and privacy resistance, restore, portability custody transfer, and deletion/revocation semantics without exporting issuer private keys. |
| EQA | One 100-case Round has 300 Institution participants with two collaborating staff each for 120 minutes, five-percent reconnect, process restart, revisions, and final-minute irreversible sealing. Then run 30,000 deterministic scoring operations, 300 private reports, and 30 appeals; prove human adjudication, aggregate suppression below ten participants, deadline authority, participant isolation, retention, restore, and no use of learner Assessment or AI scoring. |
| Clinical and imaging interoperability | At least 100 synthetic or Institution-attested already-deidentified cases cover executable frozen FHIR R4 4.0.1 profiles, closed references, mandatory terminology bindings, complete read-only DICOMweb query/retrieve plus Retrieve Capabilities, admitted DICOM WSI and ANN objects, and OME-Zarr specification revision 0.5.2 on Zarr v3 with wire `ome.version` `"0.5"`. Official/reference fixtures and at least two independent appropriate implementations validate each claim: FHIR clients/validators, DICOMweb user agents, and DICOM/OME-Zarr readers or validators. Any residual metadata, narrative, OCR, pixel PHI, retained source UID, disallowed or unknown code, original date/time, invalid Z/D replacement, malformed geometry, unsupported dimension, or source-writeback attempt is rejected and destroyed rather than repaired; two distinct reviewers admit each immutable snapshot. Clinical authorization is evidence-only: every offer to Learning Catalog, and every offer to Research where Research use is claimed, receives a separate destination-owner accept/reject result bound to exact purpose/audience/expiry and protected source roots; replay, wrong purpose, withdrawal, expiry, retention and deletion propagation pass without cross-context SQL or writeback. No clinical or diagnostic claim follows. |
| Research | One signed noninteractive Python job runs four hours with one OCPU, 4 GB RAM, and a 20-GB workspace against a read-only Dataset Snapshot. Prove approved offline Environment Manifest admission, no arbitrary package install, no production credential, default-deny egress, quota enforcement, checkpoint/restart, escape resistance, deterministic reproduction, signed artifact review/admission, failure cleanup, and absence of production model activation or clinical claims. |
| Edge Federation | One hundred nodes spanning current through N-minus-two follow the exact [Edge Node Profile](./EDGE_NODE_PROFILE.md), remain disconnected for seven days, then drain one million events and 50 GB of pending objects within 24 hours inside a platform-wide envelope of at most ten concurrent control synchronizations and two concurrent byte transfers. Prove physical enrollment, expiring leases, bounded encrypted storage, local recovery copies, signed update/rollback, acquisition-only offline authority, hostile order/replay/conflict/key/restart cases, Platform acceptance/rejection, revoke/wipe, and explicit proof that Desktop compatibility alone is not Edge conformance. |
| Audit and operations | At least one million chained records or delivery events preserve context outbox identity, projection idempotency, partition heads, daily/release signed checkpoints, gap/replay/tamper detection, bounded logs/metrics/traces, durable local notices, alert acknowledgement, incident records, retention expiry, and evidence-package privacy. Restore and replay cannot invent source-domain truth. |
| Accessibility and supported clients | On the exact release selected for routing, execute every human-facing Platform/Trust/operations, Viewer/Library/share/annotation, Catalog/learner, Teacher Authoring/local-AI/no-model, Live/Broadcast/fallback, Assessment, Integration/Credential, EQA, Clinical Shadow, Research, Edge, portability, and administration workflow across every declared browser, OS, device family, and assistive technology. Prove discoverable routes, WCAG 2.2 AA, zoom/reflow, keyboard-only operation, screen-reader semantics, focus/error/status behavior, reduced motion, offline/reconnect/expiry/revocation handling, and no critical pointer-only path. An untested declared client or inaccessible critical workflow is not qualified. |
| Backup, PITR and cold recovery | Operate the actual production host and independent target for trusted `window_end - window_start >= 90 * 24h` with no uncovered instant, named primary and alternate operators and two complete independent 35-day expiry cycles. Preserve a rolling seven-day PITR horizon plus the preceding daily anchor interval (retain WAL for up to nine days), create block-incrementals daily, schedule full backups with proven margin below the hard 168-hour maximum gap, and preserve at least 28 days of discrete restore anchors. Automatic expiry starts on day 34; prune, crypto-expiry, verification, and the signed receipt complete with `completed_at < created_at + 35 * 24h`; equality fails. PostgreSQL uses `synchronous_commit=on`, `fsync=on`, and `full_page_writes=on` without session downgrade; power-loss and storage flush semantics are hardware-qualified. Production never holds the restic repository/decryption credential: target-side restic pulls each `PENDING_PROTECTION` object through a short-lived signed hash-addressed grant. Exercise hard five-minute data-protection faults, inventory reconciliation, Backup Target ingest read-and-append identity abuse, corruption, ransomware, storage pressure, disconnected per-rotation epoch-key crypto-expiry, key loss, quorum recovery, and replacement-host restore. Restore the actual maximum-admitted live corpus. Separately create/protect the complete 150-GB corpus as an encrypted Backup Generation, restore it into an isolated empty target, reconcile bytes/objects/hashes and measure end-to-end protection plus restore throughput; export/import alone is insufficient. Portable historical durability evidence may survive only under the signed unchanged-input equivalence rule for a separately admitted run; candidate admission never survives a selection-head change. No fixed RTO or whole-site-loss claim follows. |
| Portability | Export a separate complete 150-GB canonical corpus, import it into empty N, N-minus-one and N-minus-two Institutions on capacity-qualified import/restore storage, verify schemas, owners, hashes, policies, non-rebuildable objects and governed Achievement Credentials, and round-trip again. Reject populated-target merge; disable mappings; require integration re-registration and authenticator re-enrollment; omit expired data; revalidate Legal Holds; apply the stricter retention schedule; and prove no Authentication, Adapter, or Service Credential, session, private key, recovery material, cache, or rebuildable derivative travels. This throughput corpus is not the live object allowance of the 150-GB primary data volume. |
| Zero-cash | For trusted `window_end - window_start >= 90 * 24h` with no uncovered instant, record every PathLab-specific external cash obligation gross before credits, rebates, or promotional offsets, plus expiration and usage caps for every allowance. The union of immutable invoice/provider-statement `coverage_start..coverage_end` ranges must cover every instant of that window without a billing gap, even when a statement is issued later. The workload, backup, build, restore, DNS/certificate, connectivity, support, model, API, license, hardware, and utility ledger must show zero gross incremental charge, zero incremental cash payment, and zero projected charge across the next 12 months under the frozen load. Owned or donated assets, baseline utilities, and Institution labor are disclosed separately. An expiring credit, positive projected overage, or required paid certification makes the gate `NEGATIVE`; the initial claim is the completed 90-day window. Once 12 months exist, a separate mature result freezes `window_start = window_end - P12M` under one declared timezone/calendar and requires sorted immutable statement intervals whose union covers every instant of `[window_start, window_end]`. A trusted-time-ordered immutable history must also cover every instant and prove one unchanged Deployment Selection Receipt head, Activation Receipt head with `ACTIVATED` disposition, active Delivery State projection, and release/profile/host/account/tariff/tax/currency/allowance/workload tuple. Any head or tuple change, suspension, or inactive instant resets mature eligibility from its effective time; a new subject cannot inherit earlier account coverage. The same exact-zero gross charge, payment and forward-projection tests then apply. The mature result names only that immediately preceding rolling window; neither result is “free forever.” |
| Golden Institution journey | After all prerequisite technical and bounded-context gates succeed, execute the versioned [Golden Institution Journey](./GOLDEN_INSTITUTION_JOURNEY.md) manifest against the exact candidate. Every actor, fixture, step, command, receipt, reservation, fault, invariant, cleanup action, and expected ledger transition must resolve while governance, identity, learning import, WSI, annotations, Live Learning/media, the deterministic learning journey, Assessment/grade return, Achievement Credentials, EQA, Clinical Shadow, Research, Edge, publication, portability, deletion, backup, and cold restore run in sequence. A human narrative without machine-readable step and receipt evidence is `NOT_EVALUABLE`. |
| Release and pilot readiness | Protected checks, merge/candidate freeze, release assembly and deployment selection each emit their own receipt before P6-T30 may admit and start the 90-day exact-candidate campaign; the supervised limited pilot occurs only after the exact-release gates and Golden Journey close. The complete candidate fingerprint and current `DeploymentSelectionReceipt(SELECTED)` head must match every prerequisite and evidence head. Any head change, including `REVERTED` or nominal equivalence, invalidates admission and requires a new P6-T30; no equivalence carry-forward retains admission. The pilot uses approved non-clinical or already-deidentified material and closes every incident. `SUCCESS` establishes readiness for Platform Governance to aggregate this register into a Qualification Claim; it is neither that claim nor an Activation Receipt. |

## Cross-cutting failure campaigns

Every applicable gate includes:

- process, host and backup-target restart at pre-commit, post-commit/pre-delivery, and post-delivery boundaries;
- duplicate, delayed, reordered, missing and malicious events within the current-through-N-minus-two window;
- disk-full, inode, memory, CPU, database-pool, queue, bandwidth, storage-flush and clock pressure;
- expired, revoked, wrong-Institution, wrong-purpose and replayed Authentication Credentials, Adapter Credentials, Service Credentials, grants, tokens, keys, or leases;
- malformed, oversized, decompression-bomb, traversal, injection, hostile-document and unsupported-standard inputs;
- loss and rotation of service, signing, encryption, WebAuthn, node, adapter, backup-attestation and disconnected-media epoch keys;
- backup staleness, corruption, expiry failure and restoration from only off-host material; and
- deletion with caches, projections, exports, public-release warnings, Legal Holds, external Achievement Credentials, and deduplicated backup references.

## Evidence package

Each gate emits a sanitized, signed package containing:

- exact release commit, artifact digests, SBOMs, dependency/tool/model pins, schema versions, migration heads, configuration hashes, and applicable mitigations;
- host, OS/kernel, CPU/memory/storage/network/client and backup-target declarations without secrets or private addresses;
- frozen workload manifest, machine-readable behavior invariants, synthetic or authorized fixture identities, source rights, operators, and accountable approvers;
- start/end time, resource series, latency/error distributions, event/receipt ranges, retained raw evidence hashes, and invariant results;
- fault schedule, recovery path, cleanup, omitted or non-evaluable cases, and raw-to-summary provenance;
- the [Delivery State Ledger](./DELIVERY_STATE_LEDGER.md) subject and prior heads, result, expiry/review time, and exact invalidation rules;
- privacy confirmation that evidence contains no Authentication, Adapter, or Service Credential, PHI, private slide pixels, assessment answers, or participant-level telemetry; and
- independent technical-operator and accountable-Institution signatures, with two distinct approvers wherever separation of duty is required.

## Decision rules

- Any missing mandatory artifact or stale prerequisite yields `NOT_EVALUABLE`, never pass.
- Any frozen invariant, privacy, authority, data-loss, hard-resource, license, asset-rights, backup, key, security, clinical-boundary, zero-cash, or claim-boundary breach yields `NEGATIVE` and blocks qualification or activation.
- Passing only part of a workload yields `PARTIAL`; lowering a frozen threshold after observing the result is prohibited.
- `SUCCESS` requires every asserted threshold and invariant, including cleanup, deletion, and restore, with no disallowed waiver.
- Host, storage layout, database major, kernel/filesystem, key hierarchy, mandatory dependency/tool/model pin, protocol profile, client matrix, configuration, authority boundary, selected release, upstream route, or deployment-selection-head changes invalidate the affected evidence and trigger targeted requalification.
- A route, unit test, synthetic fixture, deployment health check, historical campaign, or isolated context success cannot substitute for the complete vertical-slice and exact-release gates.

## Pilot and activation

After every prerequisite laboratory and exact-host gate plus the Golden Institution Journey succeeds, run a supervised limited pilot using approved non-clinical or already-deidentified material, bounded participants, and a named incident team. The pilot cannot replace load, restore, standards, security, privacy, or zero-cash evidence. Review every alert, support event, deletion, backup, cost, standards, and capacity receipt before Platform Governance aggregates the complete register into the Qualification Claim. Activation is a later, separate two-person operation that records the exact deployed qualified release, profile, host, evidence heads, accepted claims, expiry/review date, rollback target, and suspension conditions.
