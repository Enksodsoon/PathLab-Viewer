# Separate durable Live Learning evidence from ephemeral state

Live Learning will persist prompts, polls, submitted questions and responses, selected submitted workspace actions, and Attendance Intervals as durable evidence. Presence, pointers, viewports, control state, temporary pins, and teaching strokes remain ephemeral, while a learner notebook stays device-local unless the learner explicitly submits selected content into a Durable Interaction.
