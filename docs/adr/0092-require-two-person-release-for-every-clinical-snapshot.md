# Require two-person release for every Clinical Shadow snapshot

Every Clinical Shadow snapshot requires separate approval by an Institution-designated privacy or data steward and a WSI-capable visual reviewer before admission. Each refresh creates a new immutable quarantined snapshot and repeats deidentification-admission validation and inspection plus both approvals; PathLab rejects rather than repairs residual identifying content, and automatic synchronization or promotion can never inherit an earlier release decision.
