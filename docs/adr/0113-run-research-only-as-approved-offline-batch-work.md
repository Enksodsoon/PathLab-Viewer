# Run Research only as approved offline batch work

Research Jobs will run as signed, noninteractive Python batches under approved offline Environment Manifests against read-only Dataset Snapshots. Arbitrary packages, interactive notebooks, production credentials, and default network egress are prohibited; outputs remain private until separately admitted by an owning context and never activate a model or create a clinical claim.
