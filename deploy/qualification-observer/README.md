This is the empty local Compose mount for the optional qualification observer
socket. Production uses a root-owned directory under `/run`. No listener starts
unless an operator launches the supervised observer.
