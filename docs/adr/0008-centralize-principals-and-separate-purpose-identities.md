# Centralize principals and separate purpose identities

Trust and Governance will own one canonical Principal, institution-scoped Memberships and Role Bindings, and separately governed Purpose Identities for learning, Research, and EQA. Email and external subject values are mappings rather than identity keys, while local authentication remains complete so OIDC, LTI, and other external identity providers stay optional.
